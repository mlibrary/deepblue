<%# Reviewed: hyrax4 -- revisit? %>
<% if Flipflop.assign_admin_set? %>
  <%# TODO: consider `Hyrax::AdminSetOptionsPresenter.select_options_for(controller: controller)` instead %>
  <% if curation_concern.class.to_s == "DataSet" %>
  <%= f.input :admin_set_id, as: :select,
      include_blank: false,
      collection: Hyrax::AdminSetOptionsPresenter.new(Hyrax::AdminSetService.new(controller)).select_options_non_default_admin,
      input_html: { class: 'form-control' } %>
  <% else %>
    <%= f.input :admin_set_id, as: :select,
      include_blank: false,
      collection: Hyrax::AdminSetOptionsPresenter.new(Hyrax::AdminSetService.new(controller)).select_options_default_admin,
      input_html: { class: 'form-control' } %>
  <% end %>
<% end %>

<%= render 'form_in_works', f: f %>
<%= render 'form_member_of_collections', f: f %>

<!--
<%# if f.object.persisted? %>
  <h2><%#= t("hyrax.works.form.in_this_work") %></h2>
  <%#= render 'form_child_work_relationships', f: f %>
<#% end %>
-->
