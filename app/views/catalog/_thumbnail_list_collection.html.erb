<div class="col-md-2">
  <%# this one is for each collection shown in the search result page %>
  <% doc = render_thumbnail_tag document %>
  <% if doc.present? %>
  <%= raw doc.gsub!('data-context-href', 'data-reference').gsub!('<img ', '<img alt="collection" ') %>
  <% end %>
</div>
