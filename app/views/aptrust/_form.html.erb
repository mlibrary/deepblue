<%= form_with(model: aptrust_status, local: true) do |form| %>
  <% if aptrust_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aptrust_status.errors.count, "error") %> prohibited this aptrust_status from being saved:</h2>

      <ul>
      <% aptrust_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
