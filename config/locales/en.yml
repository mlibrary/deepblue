# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  hello: "Hello world"
  simple_form:
    actions:
      data_set:
        edit:                           "Edit"
      email_management:
        reload_email_templates:         "Reload Email Templates"
      scheduler:
        restart:                        "Restart Scheduler"
        start:                          "Start Scheduler"
        stop:                           "Stop Scheduler"
        update_schedule:                "Update Schedule"
      work_view_documentation:
        cache_on:                       "Turn Cache On"
        cache_off:                      "Turn Cache Off"
        clear_cache:                    "Clear Cache"
        export_documentation:           "Export Documentation"
        reload_email_templates:         "Reload Email Templates"
    confirm:
      data_set:
        ingest_append_run_job:          "Are you sure you want to run the append script job?"
      scheduler:
        update_schedule:                "Are you sure you want to update the schedule?"
    email_dashboard:
      labels:
        edit_email_template_textarea:   "Edit Email Template"
    scheduler_dashboard:
      labels:
        edit_schedule_textarea:         "Edit Schedule"
